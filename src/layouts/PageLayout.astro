---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description: string;
  pageNumber?: string;
  ogImage?: string;
  canonicalURL?: string;
  sectionCount?: number;
  headerLabel?: string;
}

const {
  title,
  description,
  pageNumber,
  ogImage,
  canonicalURL,
  sectionCount,
  headerLabel,
} = Astro.props;
---

<BaseLayout title={title} description={description} pageNumber={pageNumber} ogImage={ogImage} canonicalURL={canonicalURL} sectionCount={sectionCount} headerLabel={headerLabel}>
  <div class="w-full max-w-[80ch] mx-auto px-[2ch] py-[1ch]">
    <slot />
  </div>
</BaseLayout>
