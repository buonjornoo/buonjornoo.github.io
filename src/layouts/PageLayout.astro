---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description: string;
  pageNumber?: string;
  ogImage?: string;
  canonicalURL?: string;
  sectionCount?: number;
}

const {
  title,
  description,
  pageNumber,
  ogImage,
  canonicalURL,
  sectionCount,
} = Astro.props;
---

<BaseLayout title={title} description={description} pageNumber={pageNumber} ogImage={ogImage} canonicalURL={canonicalURL} sectionCount={sectionCount}>
  <div class="w-full max-w-[80ch] mx-auto px-[2ch] py-[1ch]">
    <slot />
  </div>
</BaseLayout>
